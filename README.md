# Professional Resume

A clean, modern, and responsive HTML/CSS resume template.

## How to View
Simply open `index.html` in any web browser.

## How to Push to GitHub
1. Create a repository named `resume` on GitHub.
2. Link it: `git remote add origin https://github.com/Worldtraveler247/resume.git`
3. Push: `git push -u origin main`
